# Sepolia HTTPS RPC endpoint from an Ethereum node/provider.
SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/REPLACE_ME

# Use a dedicated testnet-only account. Never reuse a mainnet private key.
# Both 0x-prefixed and unprefixed 32-byte hex keys are accepted.
TESTNET_DEPLOYER_PRIVATE_KEY=REPLACE_ME
